#!/bin/bash
# log_monitor.sh - Monitor syslog for errors

LOG_FILE="/var/log/syslog"
ALERT_KEYWORD="error"

tail -n 20 $LOG_FILE | grep -i $ALERT_KEYWORD

if [ $? -eq 0 ]; then
  echo "Alert: Errors detected in syslog."
else
  echo "No recent errors found in syslog."
fi
